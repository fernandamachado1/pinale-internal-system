## Packages
recharts | Dashboard charts for sales and production trends
framer-motion | Smooth page transitions and micro-interactions
date-fns | Date formatting for tables and reports
lucide-react | Already in base but used heavily for icons

## Notes
Integration with Shadcn UI components for sidebar layout
Data fetching uses TanStack Query v5 with strict typing from @shared/schema
Dashboard aggregates data from sales, production, and inventory
Forms for complex relations (Product + Technical Specs) require dynamic field arrays
Portuguese (pt-BR) localization for all UI text
